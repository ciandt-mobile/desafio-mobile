<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/cast_picture"
        android:layout_width="@dimen/adapter_list_width"
        android:layout_height="@dimen/adapter_list_height"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription"
        tools:src="@tools:sample/backgrounds/scenic" />

    <View
        android:id="@+id/cast_background"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/blackAlpha"
        app:layout_constraintBottom_toBottomOf="@id/cast_picture"
        app:layout_constraintEnd_toEndOf="@id/cast_picture"
        app:layout_constraintStart_toStartOf="@id/cast_picture"
        app:layout_constraintTop_toTopOf="@id/cast_name" />

    <TextView
        android:id="@+id/cast_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="12sp"
        app:layout_constraintBottom_toTopOf="@id/cast_row"
        app:layout_constraintEnd_toEndOf="@id/cast_picture"
        app:layout_constraintStart_toStartOf="@id/cast_picture"
        tools:text="Anthony Gonzalez" />

    <TextView
        android:id="@+id/cast_row"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="@id/cast_picture"
        app:layout_constraintEnd_toEndOf="@id/cast_picture"
        app:layout_constraintStart_toStartOf="@id/cast_picture"
        tools:text="Miguel (voice)" />

</androidx.constraintlayout.widget.ConstraintLayout>